import { PasswordCharValidDirective } from './password-char-valid.directive';

describe('PasswordCharValidDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordCharValidDirective();
    expect(directive).toBeTruthy();
  });
});
