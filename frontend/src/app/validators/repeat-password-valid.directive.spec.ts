import { RepeatPasswordValidDirective } from './repeat-password-valid.directive';

describe('RepeatPasswordValidDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatPasswordValidDirective();
    expect(directive).toBeTruthy();
  });
});
